1.0:
  - rephrase and summarise blk_2m TODOs (for 2.0)
  - standarise throws to std::runtime_error
  - one single throw for 1D 
  - cleanup Doxygen leftovers
  - testy na Gentoo, Fedora
  - Release mode as default
  - remove 1D TODOs (incl Pyton and tests) + document that 0D, 2D and 3D supported
  - plik README
  - plik CREDITS
  - opts_init process flags + skip inits
  - wyjaśnić dlaczego koalscencja nie działa
  - zigzag w drops.py
  - dołek
  - PY3 compatibility (Maciek)
  - separate CMake hierarchies
  - find-libclouphxx.cmake
  - cleanup .travis.yml (make-j, OMP_NUM_THREADS)
  - h5diff tests for icicle
  - consider changing asserts into throws (incl. an assert/run-time error when no spectra in blk_2m / lgrngn)
  - check if boost root-finding works on CUDA!  (to get rid of tolerances for bisect in kappa and max-mason (+something better than bisect?)
  - accumulated rainfall logic + plots (and output) + support in Python
  - blk_2m vs. iga!

2.0:
  - ventilation as an option
  - handle multiple kappas
  - C++11 constructs in lgrngn after with new CUDA!
  - Thouron et al. supersaturation?
  - what with the invalid flag??? should chemistry/etc be done only on valid droplets???
  - how to make Python assert->exception translation work in src?
  - unifikacja predkosci opadania lgrngn vs. blk_2m ?
  - rho_w do earth.hpp?
  - cleanup file extensions for lgrngn (.ipp, .tpp, ...)
  - do CUDA asserts go away with NDEBUG?
  - czy thrust::get(tuple) nie moga zwracac referencji? (o ile nie ma jednostek to by sie przydalo
  - cond/evap separation for other micro than blk_1m?
  - make the upstream logic in blk_1m in blk_2m common!
  - initialisation with multiple dry pdfs (with multiple kappas)
